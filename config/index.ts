import { auditContent, codeContent } from "./emailContent";
import { gameServer } from "./gameServer";

export const defaultSettings = {

}

export default {
  codeContent,
  gameServer,
  auditContent
};
